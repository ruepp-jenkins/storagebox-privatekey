services:
  storagebox-key-tool:
    image: ruepp/storagebox-privatekey
    container_name: storagebox-key-tool
    ports:
      - 8080:8080
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
